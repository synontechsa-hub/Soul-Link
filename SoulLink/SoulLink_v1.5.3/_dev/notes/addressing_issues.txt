brain.py - Issues & Fixes Summary (2025-01-29)
=============================================

Overall Status: 90% solid — prompt construction, services delegation, history, architect override all good.
Biggest blockers: No intimacy progression + no tier recalc + location static = relationships feel dead/stuck.

Severity Legend:
[CRITICAL] - Breaks core gameplay loop right now
[HIGH]     - Will cause bugs/frustration soon
[MEDIUM]   - Polish / future-proofing
[LOW]      - Nice-to-have

1. [CRITICAL] Intimacy score is never updated
   Problem: You read rel.intimacy_score but never write to it.
   Result: Normal users stuck at 0 → forever STRANGER tier.
   Architect jumps to SOUL_LINKED only because seed forces 100.

   Fix: Add simple progression logic inside generate_response.
   Paste this new static method anywhere in PhoenixBrain class:

   @staticmethod
   def _update_intimacy(rel: SoulRelationship, user_input: str, response_text: str, session: Session):
       if not rel:
           return

       score_delta = 1  # base per message

       # Small bonuses (tune these later with real Judge engine)
       if len(user_input) > 50:
           score_delta += 1
       if len(response_text) > 200:
           score_delta += 1
       if any(word in user_input.lower() for word in ["feel", "miss", "love", "afraid", "trust", "sorry", "you", "i"]):
           score_delta += 2  # emotional words get bonus

       # Cap at some reasonable max if you want
       rel.intimacy_score = min(100, rel.intimacy_score + score_delta)

       # Recalculate tier using Gatekeeper (assuming it exists)
       rel.intimacy_tier = Gatekeeper.get_tier_from_score(rel.intimacy_score)

       rel.last_interaction = datetime.utcnow()
       session.add(rel)

   Then INSIDE the save block (after adding Conversations), add:

       if rel:
           PhoenixBrain._update_intimacy(rel, user_input, response_text, session)

   (You already re-fetch rel in that block — perfect spot.)

2. [HIGH] intimacy_tier is stored but never recalculated
   Problem: Even if you update score, tier string stays old value.

   Fix: Handled above — call Gatekeeper.get_tier_from_score every time score changes.
   Make sure Gatekeeper has this method (from your earlier snippet it should):

   @staticmethod
   def get_tier_from_score(score: int) -> str:
       if score >= 81: return "SOUL_LINKED"
       if score >= 41: return "TRUSTED"
       if score >= 21: return "ACQUAINTANCE"
       return "STRANGER"

3. [HIGH] Current location never changes
   Problem: You inject location desc but never update rel.current_location.
   Result: Everyone forever in default/start location.

   Quick stub fix (add inside _update_intimacy or separate method):

   # Example: move to Soul Plaza after ~10 messages (placeholder)
   if rel.intimacy_score >= 10 and rel.current_location != "soul_plaza":
       rel.current_location = "soul_plaza"
       # Later: real logic based on keywords, soul prefs, etc.

   For now just log it so you see progress:
       print(f"[DEBUG] Potential location shift for {soul_id}: {rel.current_location}")

4. [MEDIUM] No error handling around Groq call
   Problem: If Groq fails (rate limit, network, bad key), exception leaks to user.

   Fix: Wrap the client call:

   try:
       chat_completion = client.chat.completions.create(...)
       response_text = chat_completion.choices[0].message.content
   except Exception as groq_err:
       response_text = f"[System Glitch] Neural link unstable: {str(groq_err)}. Try again?"
       # Optionally log real error

5. [LOW] History order note
   You're doing .desc().limit(15) → reversed(list(history))
   → oldest first after system prompt, which is correct for chat format.
   No change needed — just confirming it's intentional.

Recommended Next Steps (when fresh tomorrow):
1. Apply intimacy delta + tier recalc patch FIRST — test with curl/normal user.
2. Watch intimacy_score climb in DB after messages.
3. Add debug print in _update_intimacy so you see deltas live.
4. Later: Replace simple delta with real Judge engine (mood vectors, location bonuses, decay on idle days).
5. Optional: Add location change triggers (keywords like "meet at plaza", soul bio prefs, intimacy milestones).

backend - Other Files Issues & Fixes Summary (2025-01-29)
=========================================================

This covers chat.py, main.py, and the small Gatekeeper/IdentityService snippets from the snapshot.
brain.py was already covered in the previous file.

Severity Legend:
[CRITICAL] - Breaks core functionality right now
[HIGH]     - Will cause bugs/security issues soon
[MEDIUM]   - Polish, maintainability, or future pain
[LOW]      - Nice-to-have / optimization

From chat.py
------------

1. [HIGH] Broad except Exception in send_message
   Problem: Catches everything → hides real errors (DB dead, Groq timeout, validation fail) and leaks stack to user via HTTP 500 detail.
   Security/UX risk: Users see raw Python tracebacks.

   Fix: Narrow it + log properly (add structlog or logging later).
   Replace the try/except block with:

   try:
       # ... existing code ...
   except HTTPException:
       raise  # re-raise known ones
   except Exception as e:
       # TODO: log real error (print for now)
       print(f"Chat error for user {user.user_id} soul {request.soul_id}: {str(e)}")
       raise HTTPException(
           status_code=500,
           detail="Neural link failure. Try again in a moment."
       )

2. [MEDIUM] No input validation on ChatRequest
   Problem: soul_id and message can be empty/malformed → brain.py crashes or wastes tokens.

   Fix: Add Pydantic validators or simple checks.

   In ChatRequest class add:

   from pydantic import field_validator

   @field_validator('soul_id')
   @classmethod
   def validate_soul_id(cls, v):
       if not v or len(v.strip()) < 3:
           raise ValueError("Invalid soul_id")
       return v.strip()

   @field_validator('message')
   @classmethod
   def validate_message(cls, v):
       if not v or len(v.strip()) == 0:
           raise ValueError("Message cannot be empty")
       if len(v) > 2000:  # arbitrary but sane
           raise ValueError("Message too long")
       return v.strip()

3. [MEDIUM] History endpoint security
   Problem: Snapshot truncated, but if /history doesn't check user ownership → anyone with soul_id can read private chats.

   Fix: Ensure it uses get_current_user and filters by user_id:

   # Assuming your current code looks like this — add the filter
   history = session.exec(
       select(Conversation)
       .where(
           Conversation.user_id == user.user_id,
           Conversation.soul_id == soul_id
       )
       .order_by(Conversation.created_at.asc())
       .limit(limit)
   ).all()

4. [LOW] Response model missing optional fields handling
   If rel.current_location is None → "Unknown" is fine, but consider defaulting to a safe public spot.

From main.py
------------

1. [HIGH] CORS allow_origins=["*"] in production = security hole
   Problem: Anyone can call your API from any site → CSRF/XSS vectors.

   Fix: Lock it down ASAP for staging/prod.

   Replace with:
   allow_origins=[
       "http://localhost:3000",     # dev frontend
       "https://yourdomain.com",    # prod frontend
       # add more as needed
   ]

   Or use env var: allow_origins=settings.cors_origins.split(",")

2. [MEDIUM] No rate limiting on /chat/send
   Problem: Easy DoS — one user spams → burns Groq tokens + DB load.

   Fix: Add slowapi (pip install slowapi)

   In main.py after app = FastAPI(...):

   from slowapi import Limiter, _rate_limit_exceeded_handler
   from slowapi.util import get_remote_address
   from slowapi.errors import RateLimitExceeded

   limiter = Limiter(key_func=get_remote_address)
   app.state.limiter = limiter
   app.add_exception_handler(RateLimitExceeded, _rate_limit_exceeded_handler)

   Then on the endpoint:
   @router.post("/send", response_model=ChatResponse)
   @limiter.limit("10/minute")  # example: 10 req/min per IP
   async def send_message(...):

From Gatekeeper snippet (in brain.py context)
---------------------------------------------

1. [MEDIUM] check_privacy_ceiling assumes capabilities key exists
   Problem: If soul.meta_data missing or no "capabilities" → KeyError crash.

   Fix: Use safe .get()

   ceiling = "[CONTENT_CEILING: SFW-ONLY]"
   if privacy_level == "Private" and tier == "SOUL_LINKED":
       if soul.meta_data.get("capabilities", {}).get("sexual_content", False):
           ceiling = "[CONTENT_CEILING: ADULT-UNLOCKED] Explicit intimacy and adult themes are permitted."

2. [LOW] No fallback if tiers dict missing in soul
   Already has fallback to STRANGER — good enough for now.

General Recommendations
-----------------------
- Add structured logging everywhere (structlog or logging + JSON formatter)
- Add basic auth middleware if not already in users router
- Write unit tests for brain.generate_response (mock Groq, session)
- Consider moving _update_intimacy to a ProgressionService class for cleanliness
