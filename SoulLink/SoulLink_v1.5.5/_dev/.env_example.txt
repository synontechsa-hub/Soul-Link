# ==========================================
# SoulLink v1.5.5 - Environment Configuration Template
# ==========================================
# INSTRUCTIONS:
# 1. Copy this file to the project root as `.env`
# 2. Copy this file to `frontend/.env`
# 3. Replace all [REDACTED] values with your actual credentials
# 4. NEVER commit the actual .env files to git (they're in .gitignore)
# ==========================================

# ==========================================
# GROQ API (AI Language Model)
# ==========================================
# Get your API key from: https://console.groq.com/keys
GROQ_API_KEY=[REDACTED]

# ==========================================
# SUPABASE DATABASE (Backend Infrastructure)
# ==========================================
# The Transaction Pooler (Port 6543) - For backend async connections
# Format: postgresql://postgres.[PROJECT_ID]:[PASSWORD]@aws-1-eu-west-1.pooler.supabase.com:6543/postgres?sslmode=require
SUPABASE_DB_URL=postgresql://postgres.[PROJECT_ID]:[PASSWORD]@[REGION].pooler.supabase.com:6543/postgres?sslmode=require

# The project REST endpoint
SUPABASE_URL=https://[PROJECT_ID].supabase.co

# ==========================================
# SECURITY & AUTHENTICATION
# ==========================================
# Supabase Anon Key (Public key for client-side auth)
SUPABASE_ANON_KEY=[REDACTED]

# Supabase JWT Secret (Backend only - for token verification)
SUPABASE_JWT_SECRET=[REDACTED]

# ==========================================
# ARCHITECT MODE (God Mode / Admin)
# ==========================================
# Your personal Supabase UUID (Find in: Supabase Dashboard > Auth > Users)
ARCHITECT_UUID=[YOUR_SUPABASE_USER_UUID]

# ==========================================
# ENGINE FLAGS
# ==========================================
SOULLINK_DEBUG=0
VERSION=1.5.5
ENVIRONMENT=development

# ==========================================
# SENTRY (Error Monitoring - Optional)
# ==========================================
# Get your DSN from: https://sentry.io
# Leave blank to disable error tracking
SENTRY_DSN=[REDACTED_OR_LEAVE_BLANK]

# Production frontend URL for CORS (only needed in production)
PRODUCTION_FRONTEND_URL=[REDACTED_OR_LEAVE_BLANK]

# ==========================================
# REDIS (Caching - Optional)
# ==========================================
ENABLE_REDIS_CACHE=False
UPSTASH_REDIS_REST_URL=[REDACTED_OR_LEAVE_BLANK]
UPSTASH_REDIS_REST_TOKEN=[REDACTED_OR_LEAVE_BLANK]
REDIS_URL=[REDACTED_OR_LEAVE_BLANK]

# ==========================================
# FRONTEND-SPECIFIC (frontend/.env only)
# ==========================================
# API Base URL for AppConfig (defaults to http://127.0.0.1:8000 if not set)
API_BASE_URL=http://127.0.0.1:8000

# ==========================================
# NOTES
# ==========================================
# - Backend .env: Place in project root (SoulLink_v1.5.5/.env)
# - Frontend .env: Place in frontend folder (SoulLink_v1.5.5/frontend/.env)
# - Both files can share the same Supabase credentials
# - GROQ_API_KEY is only needed by backend
# - API_BASE_URL is only needed by frontend
