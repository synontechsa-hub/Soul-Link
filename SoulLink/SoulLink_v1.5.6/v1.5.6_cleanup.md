# SoulLink v1.5.6 — Production Cleanup

**Goal:** Strip everything that doesn't belong in a shipped production build.  
**Rule:** Delete items in the order listed. Section A is safe to auto-delete. Section B needs a quick review before deleting.

---

## A — Safe to Delete Immediately

These are build artifacts, test output files, and cache directories that should never be committed or shipped.

### A1 — Root-Level Test Output (Garbage from test runs)

| File | Size | Why Remove |
|---|---|---|
| `test_results.txt` | ~2.3 MB | Raw pytest output dump |
| `test_failures.txt` | ~369 KB | Pytest failure logs |
| `test_failures_utf8.txt` | ~178 KB | Duplicate failure log |
| `test_output.txt` | ~26 KB | Pytest stdout capture |
| `test_output_utf8.txt` | ~13 KB | Duplicate stdout capture |
| `test_run_output.txt` | ~23 KB | Another run dump |
| `test_run_targeted.txt` | ~10 KB | Targeted test run dump |
| `coverage.xml` | ~50 KB | Test coverage XML report |
| `error.log` | ~23 KB | Old error log |
| `error_clean.log` | ~11 KB | Cleaned error log |
| `tables_out.txt` | ~462 B | DB inspection output |
| `tmp_pytest_out.txt` | 0 B | Empty temp file |
| `file` | 0 B | Mystery empty file |

**PowerShell one-liner to nuke all of these:**

```powershell
cd d:\Coding\SynonTech\SoulLink_v1.5.6
Remove-Item test_results.txt, test_failures.txt, test_failures_utf8.txt, test_output.txt, test_output_utf8.txt, test_run_output.txt, test_run_targeted.txt, coverage.xml, error.log, error_clean.log, tables_out.txt, tmp_pytest_out.txt, file -ErrorAction SilentlyContinue
```

---

### A2 — Root-Level Dev Scripts (Don't belong at root)

| File | Why Remove |
|---|---|
| `fix_db_schema.py` | One-off fix script — superseded by seeders |
| `test_lifespan.py` | Dev test script at root instead of `backend/tests/` |
| `test_narrator.py` | Dev test script at root instead of `backend/tests/` |

```powershell
Remove-Item fix_db_schema.py, test_lifespan.py, test_narrator.py
```

---

### A3 — All `__pycache__` Directories (15 found)

Python bytecode cache — auto-regenerated, never ship.

```powershell
# Nuke all __pycache__ dirs recursively:
Get-ChildItem -Path . -Filter "__pycache__" -Recurse -Force | Remove-Item -Recurse -Force
```

Directories that will be removed:

```
__pycache__\
_dev\migrations\__pycache__\
_dev\migrations\versions\__pycache__\
_dev\scripts\__pycache__\
backend\app\__pycache__\
backend\app\api\__pycache__\
backend\app\core\__pycache__\
backend\app\database\__pycache__\
backend\app\logic\__pycache__\
backend\app\middleware\__pycache__\
backend\app\models\__pycache__\
backend\app\services\__pycache__\
backend\app\workers\__pycache__\
backend\scripts\__pycache__\
backend\tests\__pycache__\
```

---

### A4 — Coverage HTML Report

| Path | Why Remove |
|---|---|
| `htmlcov/` | HTML coverage report folder — large, auto-generated by pytest |

```powershell
Remove-Item htmlcov -Recurse -Force
```

---

### A5 — Empty Directories

| Path | Why Remove |
|---|---|
| `test_logs/` | Empty |
| `_dev/data_backups/` | Empty |

```powershell
Remove-Item test_logs, _dev\data_backups -Recurse -Force
```

---

### A6 — `.pytest_cache`

```powershell
Remove-Item .pytest_cache -Recurse -Force
```

---

## B — Review Before Deleting

These contain potentially useful content. Review before removing.

### B1 — `_dev/reports/` — Old Audit Documents

| File | Notes |
|---|---|
| `SoulLink_Backend_Audit_Report_22_Feb.docx` | Old Feb 22 audit — superseded by `backend_audit.md` |
| `SoulLink_FullSystem_Audit_22_Feb.docx` | Old Feb 22 full audit — superseded |
| `backend_audit.txt` | Text version of old audit |
| `fullsystem_audit.txt` | Text version of old full audit |
| `grok_report_plan.md` | Old Grok-generated plan — superseded by `v1.5.6_plan.md` |

✅ **Safe to delete** — everything useful has been superseded by `v1.5.6_audit.md` and `v1.5.6_plan.md`.

```powershell
Remove-Item _dev\reports -Recurse -Force
```

---

### B2 — `_dev/dev_log/` — Old Dev Logs

| File | Notes |
|---|---|
| `dev_log_v1.5.3.txt` | History for old version |
| `dev_log_v1.5.4.txt` | History for old version |
| `dev_log_v1.5.5.txt` | History for old version |
| `dev_log_v1.5.6.txt` | Current dev log — keep if still adding to it |

**Recommendation:** Archive `dev_log_v1.5.6.txt` to your notes app, then delete the whole folder.

```powershell
Remove-Item _dev\dev_log -Recurse -Force
```

---

### B3 — `_dev/notes/` — Reference Notes

| File | Keep? |
|---|---|
| `character_breakdown.txt` | Dev reference — useful for content work but not production code |
| `location_categories.txt` | Dev reference |
| `routines_reference.txt` | Dev reference — can be replaced by `system_config` in DB |
| `rebrand.txt` | Old branding notes |
| `root_structure.txt` | Old structural diagram — stale |
| `radio_tv_implementation.md` | Future feature spec |
| `release_roadmap.md` | Keep for roadmap tracking |
| `store_compliance.md` | Keep — needed for itch.io/App Store submission |
| `vision_manifest.md` | Keep — project vision document |

**Recommendation:** Move `release_roadmap.md`, `store_compliance.md`, and `vision_manifest.md` to the project root or a `docs/` folder. Delete the rest.

```powershell
# Move keepers to a docs folder first:
New-Item -ItemType Directory -Path docs -Force
Move-Item _dev\notes\release_roadmap.md, _dev\notes\store_compliance.md, _dev\notes\vision_manifest.md docs\

# Then delete the rest of notes:
Remove-Item _dev\notes -Recurse -Force
```

---

### B4 — `_dev/scripts/` — Diagnostic & Old Scripts

Scripts in `_dev/scripts/` that are dev-only tools and not part of the seed pipeline:

| File | Keep? | Notes |
|---|---|---|
| `seed_souls_v156.py` | ✅ **KEEP** | Core seeder — needs fixes from Stage 4 of plan |
| `seed_locations_v156.py` | ✅ **KEEP** | Core seeder |
| `validate_seed_v156.py` | ✅ **KEEP** | Useful post-seed verification |
| `check_integrity.py` | ✅ **KEEP** | Useful for data integrity checks |
| `create_schema_v156.py` | ⚠️ Review | One-off schema creation — may conflict with Alembic |
| `diag_brain.py` | ❌ Delete | Diagnostic/debug tool — not needed in production |
| `diag_chat.py` | ❌ Delete | Diagnostic/debug tool |
| `inspect_db.py` | ❌ Delete | One-off DB inspection tool |
| `generate_breakdown.py` | ❌ Delete | Generates character breakdown notes |
| `insert_architect_v156.py` | ❌ Delete | One-off architect user insert — absorbed by seeder |
| `cli_backup.py` | ⚠️ Review | Backup utility — keep if actively used |
| `rebirth_v157.py` | ❌ Delete | Plan for v1.5.7 that shouldn't be cluttering v1.5.6 scripts |
| `test_chat_pipeline_v156.py` | ❌ Delete | Dev test that belongs in `backend/tests/` not `_dev/scripts/` |
| `potential_systems/` | ⚠️ Review | Future feature sketches — move to `docs/` if valuable |

```powershell
Remove-Item `
  _dev\scripts\diag_brain.py, `
  _dev\scripts\diag_chat.py, `
  _dev\scripts\inspect_db.py, `
  _dev\scripts\generate_breakdown.py, `
  _dev\scripts\insert_architect_v156.py, `
  _dev\scripts\rebirth_v157.py, `
  _dev\scripts\test_chat_pipeline_v156.py
```

---

### B5 — `_dev/` Root Files

| File | Keep? | Notes |
|---|---|---|
| `commands.txt` | ❌ Delete | Dev shortcut commands — stale |
| `integrity_report.txt` | ❌ Delete | Old integrity run output |
| `integrity_report_utf8.txt` | ❌ Delete | Duplicate |

```powershell
Remove-Item _dev\commands.txt, _dev\integrity_report.txt, _dev\integrity_report_utf8.txt
```

---

### B6 — Root `.env` file

| File | Action |
|---|---|
| `.env` | **DO NOT DELETE** — but DO NOT ship in production image |
| `.env_example.txt` | **KEEP** — rename to `.env.example` for documentation |

Make sure `.env` is in `.gitignore` and is excluded from any Docker `COPY` commands.

```powershell
# Verify it's in .gitignore:
Select-String -Path .gitignore -Pattern "\.env"
```

---

## C — The `_dev/` Folder After Launch

Once seeding is complete (v1.5.6 plan Stage 5), the entire `_dev/` folder becomes a **developer-only tool**. It should:

- ✅ Live in source control (for dev team use)
- ❌ NOT be included in the production Docker image or itch.io build
- ❌ NOT be needed to run the app (once Stage 6 backend fixes are done)

Add to your Docker `.dockerignore` and any build exclusion list:

```
_dev/
__pycache__/
*.pyc
*.log
htmlcov/
.pytest_cache/
test_*.txt
coverage.xml
```

---

## Summary — Cleanup Totals

| Category | Files/Dirs | Est. Space Freed |
|---|---|---|
| Test output files (root) | 13 files | ~3.1 MB |
| `__pycache__` dirs | 15 dirs, 70+ .pyc files | ~2 MB |
| `htmlcov/` | 1 dir | ~1 MB |
| `.pytest_cache/` | 1 dir | ~100 KB |
| Old audit reports | 5 files | ~130 KB |
| Old dev logs | 4 files | ~37 KB |
| Diagnostic scripts | 7 files | ~30 KB |
| Misc dev files | 5 files | ~15 KB |
| Empty dirs | 2 dirs | 0 |
| **Total** | **~125 files/dirs** | **~6+ MB** |
